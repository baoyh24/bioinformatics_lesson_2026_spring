# 生物信息学作业 2.1：Linux basic command

## 作业题目

1. 对于示例文件（test_command.gtf），尝试使用相关命令或命令组合分别统计文件的行数以及字符数。
2. 利用 `grep` 等命令尝试筛选并输出示例文件中以 chr_ 起始，并且基因id为 YDL248W 的行。
3. 利用 `sed` 等命令将示例文件中的 chr_ 替换为 chromosome_ 并输出每行的第1，3，4，5列。（无需改动原文件，只输出结果）
4. 通过`man`命令以及更多的资料学习简单的 `awk` 命令，尝试互换示例文件的第2列和第3列，并且对输出结果利用 `sort` 命令依照第4和第5列数字大小排序，将最终结果输出到result.gtf文件中。
5. 更改示例文件的权限，使得文件所有者及所在用户组用户可读、写、执行而其他用户只可读，展示权限修改前后的权限变化。



## 练习笔记

- 对文件进行操作时需先进入相应的最低一级目录。注意不要遗漏路径起始的“/”。可随时通过`pwd`（当前目录），`ls和tree`确认文件
- `ls -hl`输出当前目录的信息。包括大小，权限，修改时间等。详见https://chat.deepseek.com/share/sxpc0vh6lstua7xxiy。文件的默认权限是666，目录的默认权限是777，与shell的初始设定有关
- 在对文件进行修改、建立和删除的过程中，宿主机挂载的文件夹也会同步更新
- wc -lm file_name可同时查看文件的行数和字符数
- | 管道，表示逻辑与关系，可以用于组合多个条件，例如grep `'^chr_' test_command.gtf | grep 'YDL248W'`表示同时查询以chr\_开头的行和包含YDL248W的行。只有第一个命令需要写文件名。
- 各类操作如果没有写出>或>>file_name，均不会对原文件产生影响。> 表示将输出内容创建为新文件，>>表示将内容直接添加到原文件末端，均非原地改写。原地改写的方法详见https://chat.deepseek.com/share/3cbeakk017sgqdb8ru
- 仅有root用户可以修改文件权限。可以通过powershell通过`docker exec -it --user root 容器名 bash`指令指定以root身份启动容器。（其他解决方案？）
- 使用awk对列进行互换的操作：`awk 'BEGIN{OFS="\t"}{ temp = $2; $2 = $3; $3 = temp; print }' test_command.gtf`。awk命令书写方法详见https://chat.deepseek.com/share/o6yvpighnnu6k4kyka



## 作业内容

第一题：

命令：cd /home/test/share

输出：无

命令：wc -l test_command.gtf

输出：8 test_command.gtf

命令：wc -c test_command.gtf

输出：636 test_command.gtf

（以上两命令也可统一由wc -lm test_command.gtf代替，输出 8 636 test_command.gtf）

第二题：

命令：grep '^chr_' test_command.gtf | grep 'YDL248W'

输出：chr_IV  ensembl gene    1802    2953    .       +       .       gene_id "YDL248W"; gene_version "1";
chr_IV  ensembl transcript      802     2953    .       +       .    gene_id "YDL248W"; gene_version "1";
chr_IV  ensembl start_codon     1802    1804    .       +       0    gene_id "YDL248W"; gene_version "1";

第三题：

命令：sed 's/chr_/chromosome\_/g' test_command.gtf | cut -f 1,3,4,5

输出：

chromosome_IV   gene    1802    2953
chromosome_IV   transcript      802     2953
chromosome_IV   exon    1802    2953
chromosome_IV   CDS     1802    950
chromosome_IV   start_codon     1802    1804
chromosome_IV   stop_codon      2951    2953
chromosome_IV   gene    762     3836
chromosome_IV   transcript      3762    836

第四题：

命令：awk 'BEGIN{OFS="\t"}{ temp = $2; $2 = $3; $3 = temp; print }' test_command.gtf | sort -t $'\t' -k4,4n -k5,5n > result.gtf

输出：无

命令：cat result.gtf

输出（查看排序结果，按第4、5列升序且第4列优先）：

chromosome_IV   gene    ensembl 762     3836    .       +       .    gene_id  "YDL247W-A";    gene_version    "1";
chr_IV  transcript      ensembl 802     2953    .       +       .    gene_id  "YDL248W";      gene_version    "1";
chromosome_IV   CDS     ensembl 1802    950     .       +       0    gene_id  "YDL248W";      gene_version    "1";
chr_IV  start_codon     ensembl 1802    1804    .       +       0    gene_id  "YDL248W";      gene_version    "1";
chr_IV  gene    ensembl 1802    2953    .       +       .       gene_id       "YDL248W";      gene_version    "1";
chromosome_IV   exon    ensembl 1802    2953    .       +       .    gene_id  "YDL248W";      gene_version    "1";
chromosome_IV   stop_codon      ensembl 2951    2953    .       +    0gene_id "YDL248W";      gene_version    "1";
chr_IV  transcript      ensembl 3762    836     .       +       .    gene_id  "YDL247W-A";    gene_version    "1";

第五题：

命令：ls -hl

输出：-rwxrwxrwx 1 root root 636 Mar  5 05:57 test_command.gtf

命令：chmod 774 test_command.gtf

输出：无

命令：ls -hl

输出：-rwxrwxr-- 1 root root 636 Mar  5 05:57 test_command.gtf



